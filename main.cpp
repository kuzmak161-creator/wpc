#include "common.h"                                        #include <iostream>                                        #include <vector>                                          #include <thread>                                          #include <cmath>                                                                                                      void force_quit(GtkWidget *, gpointer) { std::_Exit(0); }  static gboolean on_delete(GtkWidget *, GdkEvent *, gpointer) { std::_Exit(0); return TRUE; }                                                                                     static gboolean on_button_press(GtkWidget *, GdkEventButton *event, gpointer data) {                                      App *app = (App*)data;                                     if (event->button == 1) {                                      gtk_window_get_position(GTK_WINDOW(app->window), &app->win_start_x, &app->win_start_y);                               app->drag_start_x = (gint)event->x_root;                   app->drag_start_y = (gint)event->y_root;                   return TRUE;                                           }                                                          return FALSE;                                          }                                                                                                                     static gboolean on_button_move(GtkWidget *, GdkEventMotion *event, gpointer data) {                                       App *app = (App*)data;                                     if (event->state & GDK_BUTTON1_MASK) {                         gint dx = (gint)event->x_root - app->drag_start_x;         gint dy = (gint)event->y_root - app->drag_start_y;         gtk_window_move(GTK_WINDOW(app->window), app->win_start_x + dx, app->win_start_y + dy);                               return TRUE;                                           }                                                          return FALSE;                                          }                                                                                                                     static void on_realize(GtkWidget *widget, gpointer) {          GdkDisplay *display = gdk_display_get_default();           GdkMonitor *monitor = gdk_display_get_primary_monitor(display);                                                       GdkRectangle geo;                                          gdk_monitor_get_geometry(monitor, &geo);                   const int win_w = 360, win_h = 540;                        gtk_window_move(GTK_WINDOW(widget),                            std::max(0, geo.width - win_w - 15),                       std::max(0, geo.height - win_h - 50));             }                                                                                                                     gboolean on_tick(gpointer data) {                              App *app = (App*)data;                                                                                                if (app->mtx.try_lock()) {                                     if (app->spinning_changed) {                                   app->spinning = app->spinning_next;                        app->spinning_changed = false;                         }                                                          if (app->status_changed) {                                     gtk_label_set_text(GTK_LABEL(app->lbl_title), app->next_title.c_str());                                               gtk_label_set_text(GTK_LABEL(app->lbl_artist), app->next_artist.c_str());                                             app->status_changed = false;                           }                                                          if (app->cover_changed) {                                      reload_cover(app, app->next_cover_path);                   app->cover_changed = false;                            }                                                          if (app->track_switched) {                                     app->updating_time.store(true);                            gtk_range_set_range(GTK_RANGE(app->slider_time), 0, app->total_len);                                                  gtk_range_set_value(GTK_RANGE(app->slider_time), app->current_pos);                                                   app->updating_time.store(false);                           app->track_switched = false;                           }                                                          if (app->players_changed) {                                    rebuild_player_list(app, app->next_players);               app->players_changed = false;                          }                                                          app->mtx.unlock();                                     }                                                                                                                     if (app->spinning) {                                           app->angle += 0.04;                                    } else {                                                       app->angle += 0.002;                                   }                                                                                                                     if (app->angle > 2 * M_PI) app->angle -= 2 * M_PI;                                                                    if (app->spinning && app->current_pos < app->total_len) {                                                                 app->current_pos += 0.016;                                 app->updating_time.store(true);                            gtk_range_set_value(GTK_RANGE(app->slider_time), app->current_pos);                                                   app->updating_time.store(false);                       }                                                                                                                     gtk_widget_queue_draw(app->disc_area);                     return TRUE;                                           }                                                                                                                     void on_player_toggle(GtkWidget *, gpointer data) {            App *app = (App*)data;                                     gboolean shown = gtk_revealer_get_reveal_child(GTK_REVEALER(app->revealer));                                          if (!shown) {                                                  std::vector<std::string> players = get_players();          rebuild_player_list(app, players);                     }                                                          gtk_revealer_set_reveal_child(GTK_REVEALER(app->revealer), !shown);                                               }                                                                                                                     void show_settings_dialog(App *app) {                          GtkWidget *parent = app->window;                           bool temp_parent = false;                                  if (!parent || !GTK_IS_WINDOW(parent)) {                       parent = gtk_window_new(GTK_WINDOW_TOPLEVEL);              gtk_window_set_default_size(GTK_WINDOW(parent), 1, 1);                                                                gtk_widget_set_visible(parent, FALSE);                     temp_parent = true;                                    }                                                                                                                     GtkWidget *dialog = gtk_dialog_new_with_buttons(               "Настройки PW-menu",                                       GTK_WINDOW(parent),                                        GTK_DIALOG_MODAL,                                          "Сохранить", GTK_RESPONSE_OK,                              "Отмена", GTK_RESPONSE_CANCEL,                             nullptr                                                );                                                         gtk_window_set_default_size(GTK_WINDOW(dialog), 400, 500);                                                            gtk_window_set_resizable(GTK_WINDOW(dialog), FALSE);       apply_css(dialog, "window { background: #1a1a2e; color: #c6c6c6; }");                                                                                                            GtkWidget *box = gtk_box_new(GTK_ORIENTATION_VERTICAL, 12);                                                           gtk_widget_set_margin_start(box, 16);                      gtk_widget_set_margin_end(box, 16);                        gtk_widget_set_margin_top(box, 16);                        gtk_widget_set_margin_bottom(box, 16);                     gtk_container_add(GTK_CONTAINER(gtk_dialog_get_content_area(GTK_DIALOG(dialog))), box);                                                                                          GtkWidget *lbl_theme = gtk_label_new("Тема");              gtk_widget_set_halign(lbl_theme, GTK_ALIGN_START);         apply_css(lbl_theme, "label { color: #a4c639; font-weight: bold; }");                                                 gtk_box_pack_start(GTK_BOX(box), lbl_theme, FALSE, FALSE, 0);                                                                                                                    GtkWidget *cb_theme = gtk_combo_box_text_new();            gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_theme), "dark", "Темная");                                            gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_theme), "green", "Зеленая");                                          gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_theme), "nord", "Nord");                                              gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_theme), "gruvbox", "Gruvbox");                                        gtk_combo_box_set_active_id(GTK_COMBO_BOX(cb_theme), app->theme_name.c_str());                                        gtk_box_pack_start(GTK_BOX(box), cb_theme, FALSE, FALSE, 0);                                                                                                                     GtkWidget *lbl_audio = gtk_label_new("Аудиодрайвер");      gtk_widget_set_halign(lbl_audio, GTK_ALIGN_START);         apply_css(lbl_audio, "label { color: #a4c639; font-weight: bold; margin-top: 6px; }");                                gtk_box_pack_start(GTK_BOX(box), lbl_audio, FALSE, FALSE, 0);                                                                                                                    GtkWidget *cb_audio = gtk_combo_box_text_new();            gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_audio), "alsa", "ALSA");                                              gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_audio), "pipewire", "PipeWire");                                      gtk_combo_box_text_append(GTK_COMBO_BOX_TEXT(cb_audio), "pulse", "PulseAudio");                                       gtk_combo_box_set_active_id(GTK_COMBO_BOX(cb_audio), app->audio_driver.c_str());                                      gtk_box_pack_start(GTK_BOX(box), cb_audio, FALSE, FALSE, 0);                                                                                                                     GtkWidget *lbl_bg = gtk_label_new("Фоновое изображение (путь)");                                                      gtk_widget_set_halign(lbl_bg, GTK_ALIGN_START);            apply_css(lbl_bg, "label { color: #a4c639; font-weight: bold; margin-top: 6px; }");                                   gtk_box_pack_start(GTK_BOX(box), lbl_bg, FALSE, FALSE, 0);                                                                                                                       GtkWidget *entry_bg = gtk_entry_new();                     gtk_entry_set_placeholder_text(GTK_ENTRY(entry_bg), "Полный путь к изображению");                                     gtk_entry_set_text(GTK_ENTRY(entry_bg), app->bg_path.c_str());                                                        gtk_box_pack_start(GTK_BOX(box), entry_bg, FALSE, FALSE, 0);                                                                                                                     GtkWidget *lbl_btns = gtk_label_new("Кнопки управления");                                                             gtk_widget_set_halign(lbl_btns, GTK_ALIGN_START);          apply_css(lbl_btns, "label { color: #a4c639; font-weight: bold; margin-top: 6px; }");                                 gtk_box_pack_start(GTK_BOX(box), lbl_btns, FALSE, FALSE, 0);                                                                                                                     GtkWidget *btn_row = gtk_box_new(GTK_ORIENTATION_HORIZONTAL, 8);                                                      GtkWidget *entry_prev = gtk_entry_new();                   GtkWidget *entry_play = gtk_entry_new();                   GtkWidget *entry_next = gtk_entry_new();                   gtk_entry_set_text(GTK_ENTRY(entry_prev), app->label_prev_text.c_str());                                              gtk_entry_set_text(GTK_ENTRY(entry_play), app->label_play_text.c_str());                                              gtk_entry_set_text(GTK_ENTRY(entry_next), app->label_next_text.c_str());                                              gtk_widget_set_size_request(entry_prev, 80, -1);           gtk_widget_set_size_request(entry_play, 80, -1);           gtk_widget_set_size_request(entry_next, 80, -1);           gtk_box_pack_start(GTK_BOX(btn_row), entry_prev, FALSE, FALSE, 0);                                                    gtk_box_pack_start(GTK_BOX(btn_row), entry_play, FALSE, FALSE, 0);                                                    gtk_box_pack_start(GTK_BOX(btn_row), entry_next, FALSE, FALSE, 0);                                                    gtk_box_pack_start(GTK_BOX(box), btn_row, FALSE, FALSE, 0);                                                                                                                      gtk_widget_show_all(dialog);                                                                                          int result = gtk_dialog_run(GTK_DIALOG(dialog));           if (result == GTK_RESPONSE_OK) {                               const char *theme = gtk_combo_box_get_active_id(GTK_COMBO_BOX(cb_theme));                                             const char *audio = gtk_combo_box_get_active_id(GTK_COMBO_BOX(cb_audio));                                             if (theme) app->theme_name = theme;                        if (audio) app->audio_driver = audio;                      app->bg_path = gtk_entry_get_text(GTK_ENTRY(entry_bg));                                                               app->label_prev_text = gtk_entry_get_text(GTK_ENTRY(entry_prev));                                                     app->label_play_text = gtk_entry_get_text(GTK_ENTRY(entry_play));                                                     app->label_next_text = gtk_entry_get_text(GTK_ENTRY(entry_next));                                                                                                                if (app->audio_driver == "pipewire") flag_driver = DRIVER_PIPEWIRE;                                                   else if (app->audio_driver == "pulse") flag_driver = DRIVER_PULSE;                                                    else flag_driver = DRIVER_ALSA;                                                                                       save_config(app);                                          reload_bg(app);                                            if (app->window && GTK_IS_WINDOW(app->window)) {               gtk_widget_queue_draw(app->window);                        gtk_widget_queue_draw(app->disc_area);                 }                                                      }                                                          gtk_widget_destroy(dialog);                                if (temp_parent) {                                             gtk_widget_destroy(parent);                            }                                                      }                                                                                                                     int main(int argc, char *argv[]) {                             bool settings_only = false;                                for (int i = 1; i < argc; ++i) {                               std::string arg = argv[i];                                 if (arg == "-v" || arg == "--version") {                       std::cout << "PW-menu version 1.0 beta\n";                 return 0;                                              }                                                          if (arg == "--help" || arg == "-h") {                          std::cout << "PW-menu — Widget Player Menu\n\n"                      << "Usage: pw-menu [OPTIONS]\n\n"                          << "  -h, --help          Show help\n"                                                                                << "  -s, --settings      Open settings window\n"                                                                     << "  --swim              Floating draggable window\n"                                                                << "  --alsa              Volume via amixer (default)\n"                                                              << "  --pipewire          Volume via wpctl\n"                                                                         << "  --pulse             Volume via pactl\n"                                                                         << "  --player NAME       Use specific MPRIS player\n\n"                                                              << "Config: ~/.config/pw-menu/config.json\n";                                                               return 0;                                              }                                                          if      (arg == "--swim")     flag_swim = true;            else if (arg == "--alsa")     flag_driver = DRIVER_ALSA;                                                              else if (arg == "--pipewire") flag_driver = DRIVER_PIPEWIRE;                                                          else if (arg == "--pulse")    flag_driver = DRIVER_PULSE;                                                             else if (arg == "--player" && i+1 < argc) g_player_id = argv[++i];                                                    else if (arg == "-s" || arg == "--settings") settings_only = true;                                                }                                                                                                                     gtk_init(&argc, &argv);                                                                                               App app;                                                   load_config(&app);                                                                                                    if (flag_driver == DRIVER_ALSA && app.audio_driver == "pipewire") flag_driver = DRIVER_PIPEWIRE;                      else if (flag_driver == DRIVER_ALSA && app.audio_driver == "pulse") flag_driver = DRIVER_PULSE;                                                                                  reload_bg(&app);                                                                                                      if (settings_only) {                                           app.window = gtk_window_new(GTK_WINDOW_TOPLEVEL);          gtk_window_set_default_size(GTK_WINDOW(app.window), 1, 1);                                                            gtk_widget_set_visible(app.window, FALSE);                 g_signal_connect(app.window, "destroy", G_CALLBACK(force_quit), nullptr);                                             show_settings_dialog(&app);                                gtk_main();                                                return 0;                                         